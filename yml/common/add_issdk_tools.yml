middleware.issdk.sensor.tools:
  section-type: component
  meta-name: middleware.issdk.sensor.tools
  meta-path:
    - path: middleware/issdk/issdk_tools.meta
  contents:
    files:
      - source:  middleware/issdk/tools/Serial_To_MQTT/CloudBridgeNXPDemo.py
    component_info:
      __common__:
        description: "ISSDK Serial to MQTT bridge application"
        manifest_name: issdk_tools #IDE will show this as component name
      meta:
        type: other
        user_visible: false
        ignore_dependency: true